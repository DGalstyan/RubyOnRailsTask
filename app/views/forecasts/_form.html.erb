<%= form_for(forecast) do |f| %>
  <% if forecast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(forecast.errors.count, "error") %> prohibited this forecast from being saved:</h2>

      <ul>
      <% forecast.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :city %><br />
    <%= f.text_field :city_and_country, name: '', class: 'city-and-country form-control', autofocus: true %>
  </div>

  <%= f.hidden_field :country_code %>
  <%= f.hidden_field :name %>
  <%= f.hidden_field :lat %>
  <%= f.hidden_field :lng %>
<% end %>
